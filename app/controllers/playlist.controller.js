Player.controller('PlaylistCtrl', function($scope, VK, Utils, State, Config) {
    /*VK.init(Config.app.itemsInPage.tracks.default * 2, 0, function(data) {
        $scope.tracks = Utils.sec2time(data.tracks.items);
        State.set('loaded');
    });*/
});